# Anthropic API Key for AI-powered company research analysis
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Session Secret for encrypting user sessions
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_generated_secret_here

# Frontend URL for CORS configuration (production only)
# Example: https://your-app.vercel.app
# Leave blank for local development
FRONTEND_URL=

# Node Environment
# Use 'development' for local, 'production' for deployed version
NODE_ENV=development
